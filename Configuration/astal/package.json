{
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "build": "bun build.js",
    "all": "bun run format && bun run build"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.24.2",
    "prettier": "^3.4.2"
  }
}
